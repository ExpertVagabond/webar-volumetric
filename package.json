{
  "name": "webar-volumetric",
  "version": "0.1.0",
  "description": "Volumetric-lite video showcase — chroma-key video on billboard with gesture controls",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "devDependencies": { "vite": "^5.0.0" },
  "dependencies": { "three": "^0.162.0" }
}
